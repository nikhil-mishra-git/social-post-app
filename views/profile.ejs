<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile - <%= user?.username || 'Guest' %></title>
    <link rel="stylesheet" href="/stylesheet/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="min-h-screen flex flex-col bg-zinc-900 text-white">

    <!-- Navbar -->
    <%- include('partials/nav') %>

    <!-- Main Container -->
    <div class="container mx-auto flex flex-col md:flex-row gap-8 p-6">

        <!-- Left Section (Profile + Create Post) -->
        <div class="w-full md:w-2/5 space-y-6">

            <!-- Profile Card -->
            <div class="bg-zinc-800 p-6 rounded-lg shadow-md border border-zinc-700 flex justify-between items-center">
                <div>
                    <h2 class="text-xl md:text-2xl font-semibold">Hello, <%= user?.username || 'Guest' %></h2>
                    <p class="text-zinc-400 text-sm mt-4">
                        <%= user?.createdAt ? new Date(user.createdAt).toLocaleDateString('en-IN', { year: 'numeric', month: 'long', day: 'numeric' }) : 'N/A' %>
                    </p>
                </div>

                <!-- Logout Button -->
                <a href="/user/logout"
                    class="border-2 border-red-700 hover:bg-red-600 hover:text-white text-red-600 font-semibold py-2 px-4 rounded-lg transition">
                    Logout
                </a>
            </div>

            <!-- Post Creation Box -->
            <%- include('partials/createpost') %>

        </div>

        <!-- Right Section (Posts) -->
        <div class="w-full md:w-3/5 space-y-5">
            <div class="flex justify-center items-center border-b border-zinc-700 py-6">
                <h2 class="text-2xl text-zinc-300 font-semibold">Your Posts</h2>
            </div>

            <!-- Posts -->
            <%- include('partials/profilepost') %>

        </div>

    </div>

</body>
</html>
